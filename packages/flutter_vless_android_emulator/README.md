# flutter_vless_android_emulator

This is a legacy compatibility package for older `flutter_vless` Android release trains.

## Current Releases

Current `flutter_vless_android` releases use the main Maven Central runtime AAR
for device and emulator ABIs. New apps should depend only on `flutter_vless`;
the Android emulator binaries are already included through the main Android
implementation package.
